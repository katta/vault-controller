spring.cloud.vault:
  host: ${vault.host:localhost}
  port: ${vault.port:8200}
  scheme: ${vault.scheme:https}
  authentication: TOKEN
  token: placeholder
  config:
      order: 10
spring:
  application:
    name: vault-springapp